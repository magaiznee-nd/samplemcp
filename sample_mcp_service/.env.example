# 이 파일은 환경 변수 설정 예시입니다.
# 실제 사용 시 이 파일을 .env로 복사하고 값을 수정하세요.

# 날씨 API 키 (개발 및 테스트용 기본값)
# 실제 서비스에서는 API 키가 API 호출에서 전달됩니다.
# 이 환경 변수는 API 호출에 API 키가 포함되지 않은 경우에만 사용됩니다.
WEATHER_API_KEY=demo_api_key

# 서버 설정
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=info
